export default {
    data() {
        return {

        }
    },
    computed: {
        ueditorFontColor() {
            return this.initTheme()
        }
    },
    methods: {
        initTheme() {
            return '#000'
        }
    }
}
